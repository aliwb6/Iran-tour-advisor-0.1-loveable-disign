Iran Tour Advisor — Full Frontend Fix Prompt
You are working on an existing React/TanStack Router website for Iranian tourism. The site is already built and deployed on Cloudflare Workers. You need to implement the following 4 fixes completely:

FIX 1 — Package cards must navigate to a detail page
Problem: Clicking on package cards does nothing.
Solution:

Find the package card component on the packages listing page
Wrap each card with a TanStack Router <Link> pointing to /packages/$packageId
Create the route file src/routes/packages/$packageId.tsx
Use useParams() to get the package ID, find the matching package from src/data/site.ts
The detail page must display:

Hero image, title, difficulty badge
Cities and total duration
Full description
Day-by-day itinerary as a vertical timeline
Hotel and transport included? (yes/no with icons)
Outcomes — what the tourist will gain
Reviews/comments section at the bottom




FIX 2 — Guide cards must navigate to a profile detail page
Problem: Clicking on guide cards does nothing.
Solution:

Find the guide card component on the guides listing page
Wrap each card with a TanStack Router <Link> pointing to /guides/$guideId
Create the route file src/routes/guides/$guideId.tsx
The detail page must display:

Profile photo, full name, city
Visual star rating
Languages spoken and skills as badges
Bio paragraph
Suggested travel itinerary for their city
Contact button


On the guides listing page: sort guides by rating, highest first


FIX 3 — Apply custom font site-wide
Problem: The site is not using the custom Khamenei font.
Solution:

The .ttf font files are located at: /home/alireza/Desktop/KHAMENEI Fonts/
Copy all .ttf files into public/fonts/ in the project
In src/index.css, remove any Google Fonts imports and add:

css@font-face {
  font-family: 'Khamenei';
  src: url('/fonts/FONT_FILE_NAME.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
}

* {
  font-family: 'Khamenei', sans-serif !important;
}

Repeat @font-face for each .ttf file if there are multiple weights
Remove any other font-family overrides in Tailwind config or CSS variables


FIX 4 — AI Chat page full redesign
Problem: The chatbot is poorly designed and needs a complete redesign as a full page.
Solution:

Remove any sidebar/overlay chat behavior
Create or replace the chat page at route /chat
Add it to the main navigation menu as: گفتگو با هوش مصنوعی
Page layout:

Full viewport height (min-h-screen)
Right sidebar (300px): logo, short AI advisor description, 3–4 suggested question chips that auto-fill the input when clicked
Main chat area: takes remaining width, scrollable message list
Fixed input bar at bottom: growing textarea + send button


Message bubbles:

User messages aligned right, AI messages aligned left (RTL layout)
AI messages have a small bot avatar icon
Auto-scroll to latest message
Animated loading dots while waiting for response


Input placeholder (Persian): سفر ایده‌آل خود را توصیف کنید...
Style: warm beige/cream tones matching the rest of the site, uses Khamenei font, includes the site's main navigation header
AI system prompt to send on initialization:

You are a Persian-speaking AI travel advisor for Iran. Your job is to understand the user's interests, profession, and travel goals, then recommend the most suitable travel package from the site. Ask questions naturally and recommend packages by name. Always respond in Persian (Farsi).

FINAL REQUIREMENTS:

After all changes, run bun run build — must complete with zero errors
Fix any TypeScript errors found, especially in src/data/site.ts
Do not break any existing pages or functionality
Commit message: fix: package detail pages, guide detail pages, font, and chat page redesign

