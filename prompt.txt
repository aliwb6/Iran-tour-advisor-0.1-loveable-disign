"My Vercel deployment is failing with a 404 on the root page. I need you to strictly align my project structure with Vercel/Vite standards. Please perform these actions:

Relocate index.html: Move index.html from any subfolder (like src or public) to the root directory of the project.

Fix Asset Links: In index.html, ensure the script tag points correctly to the main entry point (e.g., <script type="module" src="/src/main.tsx"></script>).

Create vercel.json: Create a vercel.json file in the root with this content to handle routing:
{ "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }] }

Check package.json: Ensure the build command is exactly "build": "vite build" and the base path in vite.config.ts is not blocking the root access.

Verify Case Sensitivity: Ensure all file imports and names are lowercase where appropriate (crucial for Linux-to-Vercel deployments)."