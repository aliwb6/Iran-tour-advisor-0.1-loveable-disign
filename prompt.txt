

I need to implement the following changes and features to my existing Iran Tour Advisor website. The site is already partially built — please make these modifications:

1. Package Detail Pages (کلیک روی پکیج‌ها)
Each travel package card on the packages page must be clickable and navigate to a dedicated detail page. The detail page should include:

Full package title and description
List of cities covered
Duration of the trip
Day-by-day itinerary (what happens each day, which locations are visited)
Whether hotel and transportation are included (clearly marked)
What the tourist will gain/learn from this package (outcomes)
A comments/reviews section where tourists can leave their experience and rating
A "Book Now" or "Contact Us" CTA button


2. Guide Profile Detail Pages (کلیک روی راهنمایان)
Each guide card on the guides page must be clickable and open a full profile page. The guide profile page should include:

Full name and profile photo
Contact information
Languages spoken and skills/expertise
A personal bio paragraph
A suggested travel plan/itinerary they offer for their city
Star rating (average score from tourist reviews)
A section for tourists to submit a rating and review

Guides should be sorted by rating — highest rated guides appear first on the guides listing page.

3. AI Chatbot — Separate Full Page
The chatbot currently opens as a sidebar panel. This must be changed:

Remove the sidebar/overlay behavior completely
The chatbot must have its own dedicated page (e.g. /chat or /ai-advisor)
It should be accessible from the main navigation menu as a full page, styled consistently with the rest of the site
The chatbot should ask users about their interests, profession, goals, and travel preferences, then recommend the most suitable package from the site's offerings
If no exact match exists, it should suggest the closest available package
The chatbot must be connected to Claude API (or another major AI provider) for intelligent responses


4. Custom Font — Apply Site-wide
The entire website must use a custom Persian font. The font files are located at:
/home/alireza/Desktop/KHAMENEI Fonts/
This folder contains .ttf font files. Please:

Load these fonts using @font-face in the CSS
Apply this font globally across the entire site (all headings, body text, buttons, navigation, etc.)
Make sure the font renders correctly for Persian (RTL) text
Remove or override any previously used font


5. General Notes

The site is trilingual: Persian (FA), English (EN), and Arabic (AR) — ensure all new pages respect the active language and RTL/LTR direction accordingly
Design style must remain modern, elegant, and Iranian-themed — consistent with the existing design
All new pages must be fully responsive (mobile + desktop)
Maintain the existing routing structure and add new routes cleanly


این پرامپت کامله و همه چیز رو پوشش میده. اگه بخوای بخش خاصی رو بیشتر جزئی کنیم یا چیزی اضافه کنیم بگو.You said: اوپن کد کارش تموم شد ولی موقع دپلوی این ارور رو دیدم2026-05-12T12:19:33.964Z	Initializing build environment...
2026-05-12T12:19:35.848Z	Success: Finished initializing build environment
2026-05-12T12:19:36.427Z	Cloning repository...
2026-05-12T12:19:37.678Z	Detected the following tools from environment: bun@1.2.15, nodejs@22.16.0
2026-05-12T12:19:37.6pastedاوپن کد کارش تموم شد ولی موقع دپلوی این ارور رو دیدم 
اینم از لاگش :3:50 PMClaude responded: مشکل واضحه — یه syntax error در فایل src/data/site.مشکل واضحه — یه syntax error در فایل src/data/site.ts در خط ۱۴۳ هست. یه } اضافه یا جای اشتباه داره.
پرامپت زیر رو به OpenCode بده:

پرامپت برای OpenCode:

There is a build error in the project. Fix the TypeScript syntax error in src/data/site.ts.
Error:
/opt/buildhome/repo/src/data/site.ts:143:0: ERROR: Unexpected "}"
Context from build log:
141 |      date: string;
142 |    }[];
143 |  };
     |  ^
144 |  
145 |  export const guides: Guide[] = [
There is an unexpected closing brace } at line 143 in src/data/site.ts. This is likely a mismatched or extra } inside a TypeScript interface or type definition.
Please:

Open src/data/site.ts
Find the type/interface definition around lines 138–145
Fix the mismatched braces so the TypeScript is valid
Make sure the file compiles without errors
Do not change any other logic or data in the file


این یه باگ ساده‌ست، بعد از fix باید build موفق بشه.You said: سایت اونطور که گفتم بهت نشد همونطوریه و هیچ فرقی نکردهسایت اونطور که گفتم بهت نشد همونطوریه و هیچ فرقی نکرده4:17 PMClaude responded: برای اینکه بفهمم دقیقاً چه چیزی تغییر نکرده، چند تا سوال دارم: باشه، پس همه چیز هنوز درست نشده.برای اینکه بفهمم دقیقاً چه چیزی تغییر نکرده، چند تا سوال دارم:باشه، پس همه چیز هنوز درست نشده. احتمالاً OpenCode تغییرات رو اعمال کرده ولی build موفق نشده یا تغییرات به درستی push نشدن.
یه پرامپت دقیق‌تر و step-by-step بنویس و بده به OpenCode:

پرامپت کامل برای OpenCode:

The website currently has several issues that need to be fixed. Please implement ALL of the following changes carefully:

ISSUE 1 — Package cards are not clickable
Currently clicking on a package card does nothing. Fix this:

Each package card must link to a detail page at /packages/[package-id]
Create a new page component: src/pages/PackageDetail.tsx (or the equivalent route file based on the router being used)
Add this route to the router
The detail page must show: title, description, cities, duration, day-by-day itinerary, hotel/transport info, outcomes, and a reviews section


ISSUE 2 — Guide cards are not clickable
Currently clicking on a guide card does nothing. Fix this:

Each guide card must link to a detail page at /guides/[guide-id]
Create a new page component: src/pages/GuideDetail.tsx
Add this route to the router
The detail page must show: name, photo, contact info, languages, skills, bio, travel plan, and star rating
On the guides listing page, sort guides by rating (highest first)


ISSUE 3 — AI Chatbot must be a separate full page
Currently the chatbot opens as a sidebar or overlay. Change this completely:

Remove the sidebar/panel behavior
Create a dedicated page at /chat
Add "چت با هوش مصنوعی" (AI Chat) as a navigation menu item that links to /chat
The chat page must be full-width, styled like the rest of the site
Keep the existing chat functionality but display it as a full page


ISSUE 4 — Custom font not applied
The custom font has NOT been applied site-wide. Fix this:

The font .ttf files are in the project. If they are not yet added, the font folder path on the developer's machine is: /home/alireza/Desktop/KHAMENEI Fonts/
Copy the font files into public/fonts/ in the project
Add @font-face declarations in the global CSS file (e.g. src/index.css or src/styles/global.css)
Set font-family globally on body, html, * so every element uses this font
Remove all other font imports (Google Fonts, etc.)


IMPORTANT NOTES:

The project uses TanStack Router — make sure new routes are added correctly using the TanStack Router file-based or code-based routing pattern already in use
After all changes, run bun run build and make sure it compiles with zero errors
Fix any TypeScript errors that appear, especially in src/data/site.ts (there was a previous syntax error with an unexpected } near line 143)
Do not break existing pages or functionality


این بار خیلی واضح‌تر گفتیم دقیقاً چی باید بشه. بعد از اینکه OpenCode کارش تموم شد، build log رو چک کن که error نداشته باشه.You said: Q: کدوم بخش‌ها هنوز درست کار نمی‌کنن؟ (Select all that apply)Q: کدوم بخش‌ها هنوز درست کار نمی‌کنن؟ (Select all that apply)
A: کلیک روی پکیج‌ها (جزئیات باز نمیشه), کلیک روی راهنمایان (پروفایل باز نمیشه), فونت تغییر نکرده, گفت و گو با هوش مصنوعی یا همون چت بات هم اومده تو یه صفحه ی دیگه ولی خیلی بده4:19 PMClaude responded: باشه، پس ۳ تا مشکل اصلی داری.باشه، پس ۳ تا مشکل اصلی داری. بذار هر کدوم رو جدا و خیلی دقیق‌تر بنویسیم برای OpenCode.

پرامپت برای OpenCode — سه issue جداگانه:

ISSUE 1 — Package & Guide detail pages not working
The package cards and guide cards are still not navigating to detail pages when clicked. This must be fixed properly.
For Packages:

Open the packages listing page
Find the package card component
Wrap each card with a <Link to="/packages/$packageId"> (TanStack Router syntax) using the package's id field
Create the file src/routes/packages/$packageId.tsx (TanStack Router file-based routing)
In this file, read the packageId param, find the matching package from src/data/site.ts, and display:

Hero image + title
Difficulty badge (آسان/متوسط/سخت)
Cities and duration
Full description
Day-by-day itinerary as a timeline
Whether hotel and transport are included (icons + yes/no)
What the tourist gains from this package (outcomes)
A static reviews/comments section at the bottom



For Guides:

Open the guides listing page
Wrap each guide card with <Link to="/guides/$guideId">
Create src/routes/guides/$guideId.tsx
Display:

Profile photo, name, city
Star rating (visual stars)
Languages and skills as badges
Bio paragraph
Suggested travel itinerary for their city
Contact button


Sort guides by rating descending on the listing page


ISSUE 2 — Custom font not applied
The Khamenei font is still not being used. Follow these exact steps:

Locate the .ttf font files — they should already be in the project, or find them at /home/alireza/Desktop/KHAMENEI Fonts/
Place all .ttf files inside public/fonts/ directory
Open src/index.css (the global stylesheet)
Remove ANY existing @import for Google Fonts or other external fonts
Add @font-face rules like this:

css@font-face {
  font-family: 'Khamenei';
  src: url('/fonts/YOUR_FONT_FILE.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
}
(repeat for each .ttf file with different weights if they exist)

Then add globally:

css* {
  font-family: 'Khamenei', sans-serif !important;
}

Also remove any font-family set in tailwind.config or CSS variables that would override this


ISSUE 3 — AI Chat page needs complete redesign
The chat page exists but looks bad. Redesign it completely as a proper full-page chat interface:
Layout:

Full viewport height page (min-h-screen)
Right sidebar (300px) showing: site logo, short description of the AI advisor, and 3-4 suggested quick-start questions as clickable chips
Main chat area taking remaining width
Fixed input bar at the bottom of the chat area

Chat area:

Messages displayed in bubbles — user messages on the left (RTL), AI messages on the right
AI messages should have a small bot avatar/icon
Smooth scroll to latest message
Loading indicator (three animated dots) while waiting for AI response

Input bar:

Textarea (not input) that grows with content
Send button with arrow icon
Placeholder text in Persian: سفر ایده‌آل خود را توصیف کنید...

Style:

Match the site's existing color palette (warm beige/cream tones)
Use the Khamenei custom font
The page must have the site's main navigation header at the top
Make it fully responsive (on mobile, sidebar collapses or hides)

AI behavior prompt — when initializing the chat, send this as the system message:
You are a Persian-speaking AI travel advisor for Iran. Your job is to understand the user's interests, profession, and travel goals, then recommend the most suitable travel package from the site. Ask questions naturally and recommend packages by name. Always respond in Persian (Farsi).

FINAL CHECK:
After all changes, run bun run build — it must complete with zero errors before committing.